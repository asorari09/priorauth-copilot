/**
 * @deprecated Use `npm run rebuild:presets` (scripts/rebuildPresetDemoResults.ts).
 * That script loads real citations from Supabase cases — never fabricates placeholders.
 */
import "./rebuildPresetDemoResults";
